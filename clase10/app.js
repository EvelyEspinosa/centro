 var data = require('D:/modulo2.js/clase10/datosBici');
 let bici = JSON.parse(data);
console.log(bici);
console.log(typeof(bici));

    function buscarBici(id){
        bici.filter((id == id) ? id : null);
        }

console.log(buscarBici());

    function venderBici(id){
        bici.filter((id) == si != no);return "Bicicleta no encontrada";
    }
    console.log(venderBici());

    function biciParaLaVenta(id){
        bici.filter((id) == no)
        return id;
    }
    console.log(biciParaLaVenta());

        function totalDeVentas(valor1,valor2,indice,vector){
            bici.reduce(vendidas)
            return valor1 + valor2;
        }
        console.log(totalDeVentas());